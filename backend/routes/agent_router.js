import express from 'express';
import agentControllers from '../controllers/agent_controllers.js';

const agentRouter = express.Router();


agentRouter.post('/login',);

agentRouter.post('/signup', );

export default agentRouter;